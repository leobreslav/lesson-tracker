/**
 * Как часто спрашиваем сервер на живых экранах.
 *
 * Три секунды — на глаз «сразу» и при этом не нагрузка: тридцати учеников
 * и одного учителя мало, чтобы Channels с Redis окупились. Но два воркера
 * gunicorn на проде значат, что дешёвым обязан быть **ответ**, а не только
 * запрос: у обоих опрашиваемых адресов есть `?version=`, и совпавшая версия
 * стоит один агрегат вместо сборки страницы.
 *
 * Число одно на оба экрана намеренно: учитель и ученик смотрят на одну и ту
 * же работу с двух сторон, и «у кого-то обновляется быстрее» — это не
 * настройка, а несогласованность.
 */
export const POLL_MS = 3000
