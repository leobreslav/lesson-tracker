"""
Полка перестала помнить, какой из моих планов «ведут».

Пометка отвечала на вопрос «какой шаблон обновит кнопка», и вопрос был
поставлен от безысходности: кнопка искала шаблон **сама**, по предмету и
параллели, а таких находилось несколько. Ответом стала запись, и она честно
чинила догадку — но ценой ограничения на всю полку: у автора не могло быть
двух ведомых планов по одному предмету и параллели, то есть «сохранил в
сентябре, сохранил в мае» упиралось в отказ.

Теперь шаблон для перезаписи **называют явно**, и угадывать нечего: пометка
осталась без вопроса, на который отвечала. Ограничение уходит вместе с ней.

Обратного хода нет намеренно. Вернуть поле мало — надо решить, какой из
шаблонов автора живой, а ответа на это в данных больше нет: полка с двумя
записями по одному предмету теперь законна, и любой выбор был бы той самой
догадкой по алфавиту, от которой уходили.
"""

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0005_delete_plantemplaterow'),
    ]

    operations = [
        migrations.RemoveConstraint(
            model_name='plantemplate',
            name='one_live_template_per_subject_and_grade',
        ),
        migrations.RemoveField(
            model_name='plantemplate',
            name='is_live',
        ),
    ]
