"""
У вопроса появляется своё имя.

Номер по порядку отвечал сразу на два вопроса — «какой он по счёту» и «как он
называется», — и на работе с пунктами «1а, 1б, 1в» это ломалось: три пункта
одного задания превращались в три задачи с номерами 1, 2, 3, а сказать «в
третьей задаче» учитель уже не мог — третьей у него нет.

Заполнять поле номером задним числом не надо и нельзя: пустое имя значит «зовусь
своим номером», и это верно для всех существующих вопросов. Заполненное число
пришлось бы перебивать при каждом переносе вопроса вверх или вниз.
"""

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("works", "0026_work_stops_being_paper"),
    ]

    operations = [
        migrations.AddField(
            model_name="task",
            name="label",
            field=models.CharField(
                blank=True,
                default="",
                help_text=(
                    "Как вопрос назван в работе: «1а», «324 из Галицкого». "
                    "Пусто — зовётся своим номером по порядку."
                ),
                max_length=16,
                verbose_name="label",
            ),
        ),
    ]
