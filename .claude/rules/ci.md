---
paths:
  - ".github/workflows/**"
  - "e2e.sh"
---

## Проверка на GitHub — те же три уровня

`.github/workflows/tests.yml` гоняет **тот же docker compose**, а не
собранную рядом «нативную» установку: второй способ запустить те же тесты
однажды разошёлся бы с первым, и узнали бы мы об этом по зелёному CI при
красном ноутбуке.

| событие | что идёт |
|---|---|
| пуш любой ветки и PR | питон, узловые, `./e2e.sh --smoke` |
| **пуш в `main`** | то же **плюс полный браузерный**, четырьмя шардами |
| ночью (cron) и по кнопке | полный браузерный, четырьмя шардами |

Полный на `main` — это дверь перед продом, и стоит она здесь, а не на
ноутбуке: `main` — ветка, с которой двигают `production`, поэтому проверять
надо именно её, и проверять целиком. На ветках и в пул-реквестах хватает
дымового — их ещё никто не выкатывает, а раннеры не бесплатны.

Почему дверь уехала с ноутбука: там те же тесты шли 15.7 и 17.1 минуты
вместо задокументированных 8.5 — стек разработки и поднимаемый тестовый
делят восемь гигабайт, — и все семь отказов из этих двух прогонов оказались
таймаутами `page.goto`, а не поломками. Четырьмя шардами то же самое идёт
6 минут. Замер — `docs/why-these-rules.md`.

**Отсюда обязанность: прод двигается на коммит с зелёным прогоном**, и
смотреть надо прогон того самого коммита (`gh run watch`,
`gh run list --branch main --limit 1`). Стенд зелёности не ждёт — он тянет
`main` каждые три минуты, и это осознанно: стенд для того и заведён, чтобы
на нём ломалось.

Шарды разложены `--shard=N/4`, и e2e.sh для этого править не пришлось: всё,
чего он не разобрал сам, уезжает в playwright насквозь. Одинаковое имя
compose-проекта им не мешает — шарды идут на разных раннерах; на одной
машине два прогона по-прежнему невозможны.

**Одна правка в e2e.sh для этого понадобилась**: `docker compose run` без
терминала спотыкается о «the input device is not a TTY», поэтому `-T`
подставляется, когда терминала нет. Локально флаг не ставится — с ним
пропадает цвет и посекундный вывод, а смотрят на него глазами.

Раннер начинает с чистого клона без единого ключа, то есть ровно с того, с
чего начинается облачная сессия. Поломку «сторож пустоты падает без ключей
R2» такой прогон нашёл бы сам, и в этом главная его ценность помимо
зелёной галочки.
